; Prefix the `npm version` commit + tag message so ci.yml can skip the branch
; push of a release commit (the tag still drives release.yml -> no double run).
; See the job-level `if:` guards in .github/workflows/ci.yml.
message = "chore(release): %s"
